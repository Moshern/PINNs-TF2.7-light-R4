# Python caches
__pycache__/
*.py[cod]
*.egg-info/

# Large generated/intermediate files
*.mat.bak
.idea/
.vscode/

# Full CFD training file (too large for GitHub); only the extracted mesh is tracked
data/bendpipe/Re1000Wo10_noise5_data5_pinn.mat

# Intermediate checkpoints saved every N epochs (only the final weights are tracked)
weights/*/checkpoint-*
weights/*/*_epoch_*

# Prediction outputs
predict_results/
